// Default Export
// - Hanya bisa mempunyai 1 default export per file
export default function sum(a, b) {
    return a + b;
}

// The Default Import
/* 
    - Tidak memakai curly braces ( {} ) pada saat menggunakan default imports 
*/
// import sum from './defaultModule.js';